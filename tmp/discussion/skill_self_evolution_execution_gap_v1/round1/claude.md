The analysis and plan are ready. The plan file is at `/Users/mac/.claude/plans/wise-exploring-acorn.md`.

**Summary of the evaluation:**

The template's governance layer is honest and structurally sound. The execution layer is the actual problem — skills are designed to be reviewable, not to improve through use. The freeze-plan recommendation is Direction B, constrained to four incremental moves that close the execution gap without pretending unsupported automation exists:

1. Add `evolution_mode: FIX | DERIVED | CAPTURED` to the candidate packet template (highest-value OpenSpace import, schema-only)
2. Define a trigger-eval I/O interface contract (no engine, just declared schema + stub)
3. Add `invocation_id` linkage to closeout receipts (schema-only lineage anchor)
4. Write `harvest_candidates.py` — read-only, evidence-tier filtered, no canonical mutation

The one OpenSpace idea to resist is metric-driven autonomous promotion triggers — they presuppose a telemetry infrastructure this template doesn't have, and wiring them to constitutional fields (purpose, triggers, governance) would create a false authority chain before any metric source is verified.
